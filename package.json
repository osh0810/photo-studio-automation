{
  "name": "studio-task-safeguard",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local",
    "start": "wrangler dev --local",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "migrate": "tsx scripts/migrate-customers.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.6.0",
    "dotenv": "^17.4.2",
    "tsx": "^4.21.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.83.0"
  }
}
